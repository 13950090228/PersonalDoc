
namespace Campfire.Sound {
    // 声音类型
    public enum SoundType {
        Normal,
        Ambient_Simple,
        Ambient_Large,
    }
    
    // 声音监听者类型
    public enum AudioListenerType {
        None = 0,
        Camera = 1,
        Player = 2,
    }
}

